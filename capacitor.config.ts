import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'novedades.kay.biz.app',
  appName: 'novedades-katy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
